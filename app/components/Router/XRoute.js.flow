// @flow

declare type $XRouteProps = {|
  +component: Function,
  +componentProps?: {
    [prop: string]: $JSON,
  },
  +routeIndex: number,
  +style?: Object,
|};

declare type $XRouteStatus =
  | 'off'
  | 'entering'
  | 'transitionend'
  ;

declare type $XRouteState = {|
  +status: $XRouteStatus,
|};
